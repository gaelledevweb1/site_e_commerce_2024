{% extends 'base.html.twig' %}

{% block title %}ArticleBlog{% endblock %}

{% block content %}
    <h1>ArticleBlog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_blog.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ article_blog.Title }}</td>
            </tr>
            <tr>
                <th>Contain</th>
                <td>{{ article_blog.contain }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ article_blog.author }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ article_blog.dateCreation ? article_blog.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_article_blog_index') }}">back to list</a>

    <a href="{{ path('app_admin_article_blog_edit', {'id': article_blog.id}) }}">edit</a>

    {{ include('admin_article_blog/_delete_form.html.twig') }}
{% endblock %}
