{% extends 'base.html.twig' %}

{% block title %}CategoriesBlog{% endblock %}

{% block body %}
    <h1>CategoriesBlog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categories_blog.id }}</td>
            </tr>
            <tr>
                <th>NameCategory</th>
                <td>{{ categories_blog.nameCategory }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categories_blog_index') }}">back to list</a>

    <a href="{{ path('app_categories_blog_edit', {'id': categories_blog.id}) }}">edit</a>

    {{ include('categories_blog/_delete_form.html.twig') }}
{% endblock %}
