{% extends 'base.html.twig' %}

{% block title %}Inscription index{% endblock %}

{% block body %}
    <h1>Inscription index</h1>

    <table class="table">
        <thead>
            <tr>
                {# <th>Id</th>
                <th>CreatedAT</th>
                <th>UpdateAt</th> #}
                <th>Birthday</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for inscription in inscriptions %}
            <tr>
                {# <td>{{ inscription.id }}</td> #}
                {# <td>{{ inscription.createdAT ? inscription.createdAT|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ inscription.updateAt ? inscription.updateAt|date('Y-m-d H:i:s') : '' }}</td> #}
                <td>{{ inscription.birthday ? inscription.birthday|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_inscription_cours_gymnastique_senior_show', {'id': inscription.id}) }}">show</a>
                    <a href="{{ path('app_inscription_cours_gymnastique_senior_edit', {'id': inscription.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_inscription_cours_gymnastique_senior_new') }}">Create new</a>
{% endblock %}
