{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inscription.id }}</td>
            </tr>
            <tr>
                <th>CreatedAT</th>
                <td>{{ inscription.createdAT ? inscription.createdAT|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ inscription.updateAt ? inscription.updateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ inscription.birthday ? inscription.birthday|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inscription_cours_gymnastique_senior_index') }}">back to list</a>

    <a href="{{ path('app_inscription_cours_gymnastique_senior_edit', {'id': inscription.id}) }}">edit</a>

    {{ include('inscription_cours_gymnastique_senior/_delete_form.html.twig') }}
{% endblock %}
