{% extends 'base.html.twig' %}

{% block title %}Mon Compte
{% endblock %}

{% block content %}


	<h1>Mon compte
	</h1>

	{#  je souhaite avoir les informations de l'utilisateur connecter #}
	<div class="container">
		<div class="row">
			<div class="col-md-6">
				<h2>Informations personnelles</h2>
				<p>{{app.user.firstName }}</p>
				<p>{{ app.user.lastName }}</p>
				{# <p>{{app.user.birthday ? user.birthday|date('Y-m-d') : '' }}</p>  #}


				<p>{{ app.user.email }}</p>
				<p>{{ app.user.password }}</p>
				<p>{{ app.user.phone }}</p>
				<h2>Adresse</h2>

				{% for adress in user.adress %}
					{% for field in adressFields %}
						<tr>
							<th>{{ field }}</th>
							<td>{{ attribute(adress, field) }}</td>
						</tr>
					{% endfor %}

					<a href="{{ path('app_adress_new') }}">Ajouter une  addresse</a>
				{% endfor %}

				<h2>Adresse de livraison</h2>


					{# <a href="{{ adressDeliveryFormUrl }}">Modifier l'adresse de livraison</a> #}
				{# {% for adressDelivery in order.adress %}
					{% for field in adressFields %}
						<tr>
							<th>{{ field }}</th>
							<td>{{ attribute(adressDelivery, field) }}</td>
						</tr>
					{% endfor %}

					<a href="{{ path('app_adress_new') }}">Ajouter une adresse</a>
				{% endfor %} #}

				{% if app.user %}
					<a href="{{ path('app_adress_new') }}">Ajouter une adresse</a>
				{% endif %}


			</div>

			<div class="col-md-6">
				<h2>Commandes</h2>
				<p>Vous n'avez pas encore passé de commande</p>

				<h2>
					historique de vos Commandes</h2>
				<p>Aucune</p>
			</div>

		</div>
	</div>


	<a href="{{ path('app_user_index') }}">back to list</a>


	{% block footer %}
		{% include 'frag/footer.html.twig' %}
	{% endblock %}

{% endblock %}
