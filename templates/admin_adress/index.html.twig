{% extends 'base.html.twig' %}

{% block title %}Adress index{% endblock %}

{% block body %}
    <h1>Adress index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Address</th>
                <th>City</th>
                <th>Zip</th>
                <th>Country</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for adress in adresses %}
            <tr>
                <td>{{ adress.id }}</td>
                <td>{{ adress.Address }}</td>
                <td>{{ adress.City }}</td>
                <td>{{ adress.Zip }}</td>
                <td>{{ adress.Country }}</td>
                <td>
                    <a href="{{ path('app_admin_adress_show', {'id': adress.id}) }}">show</a>
                    <a href="{{ path('app_admin_adress_edit', {'id': adress.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_adress_new') }}">Create new</a>
{% endblock %}
