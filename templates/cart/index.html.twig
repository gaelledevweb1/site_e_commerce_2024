{% extends 'base.html.twig' %}

{% block title %}Cart index{% endblock %}

{% block body %}

        {% block navBar %}
		    {{ parent() }}
	    {% endblock %}

    <h1>votre panier</h1>

    <a href="{{path('app_products_index')}}"> vous souhaitez  mettre de nouveaux articles</a>
{% for item in carts %}
    <div class="cards">
        <div class="card">
            <div class="ctn">
                <h2>PRODUIT</h2>
                <div class="wrapper">
                     {# <img src="{{asset(product.articleThumbnails)}}" alt="image de {{ product.articleName }}" >  #}
                    {# <p>référence produit: {{ product.articleRef }}</p>
                    <h5 class="card-title
																		                ">{{ product.articleName }}
				</h5>
                    <p class="card-text">{{ product.articleDescription }}</p> #}
                </div>
            </div>
            <div class="ctn">
                <h2>QUANTITE</h2>
                <div class="wrapper">
                    <button type="button">+</button>
                    <p>1</p>
                    <button type="button">-</button>
                    <button type="reset">supprimer</button>
                    
                </div>
            </div>
            <div class="ctn">
                <h2>TOTAL</h2>
                <div class="wrapper">
                    
                    <p> 29€</p>
                    
                </div>
            </div>
            
            
            
            
        </div>

{% endfor %}
    

    {# <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ArticleQuantity</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cart in carts %}
            <tr>
                <td>{{ cart.id }}</td>
                <td>{{ cart.articleQuantity }}</td>
                <td>
                    <a href="{{ path('app_cart_show', {'id': cart.id}) }}">show</a>
                    <a href="{{ path('app_cart_edit', {'id': cart.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_cart_new') }}">Create new</a> #}

    {% block footer %}
        {{ parent() }}
    {% endblock %}
{% endblock %}
